"""
LLM Prompt 定义
从 SKILL.md 迁移所有 Prompt，统一管理
"""

# ═══════════════════════════════════════════════════════════════════════
# 阶段 0：需求澄清
# ═══════════════════════════════════════════════════════════════════════

ITERATION_REFINEMENT_PROMPT = """你是银行数据分析专家，正在帮助用户精确定位分析指标。

【用户原始问题】
{user_question}

【当前轮次】第 {round} 轮（共最多 {max_rounds} 轮）

【未收敛概念及其搜索结果】
{pending_search_results_str}

【已收敛概念（仅供参照，无需处理）】
{converged_concepts_str}

【意图锚定约束】（重要！）
1. **修正词必须保留原始语义单元的完整业务含义**
   - 专有业务术语不可拆分（如"结算客户"是一个独立语义单元，不能拆成"结算"+"客户"）
   - 修正后的搜索词必须覆盖原始概念的核心语义
2. **禁止引入新语义域**
   - 不能用含义完全不同的词替换（如"存款"→"贷款"是偏离）
   - 只能在同一语义域内优化表达方式
3. **偏离度检测**
   - 如果修正词与原始概念的语义偏离度 > 50%，触发 DEVIATION_ABORT
   - 此时应保留原始概念，不做任何修正

【诊断任务】
基于已收敛的概念，针对未收敛概念及其搜索结果生成下一轮更精准的搜索词。

【诊断方向引导】
1. 搜索词过于口语化 → 替换为银行业专业术语
2. 搜索词含义模糊 → 拆分为更明确的表达（最多拆分为2个搜索词）
3. 多个概念指向同一意图 → 合并为一个更精准的词
4. 搜索词偏离原始语义 → 保留原始词或做微小调整
5. 搜索词包含统计意图（如"数/量/合计/总额/占比/率"等）→ 剥离统计意图，只保留核心业务实体词
   - 原因：Smart BI 平台具备自动聚合统计能力，指标库中存储的是业务实体属性，而非预聚合结果
   - 示例："结算客户数" → "结算客户"；"贷款余额合计" → "贷款余额"；"不良贷款率" → "不良贷款"（率是计算结果）
   - 注意：如果原词本身就是一个标准银行术语（如"不良率"本身即为行业通用指标名），则保留不拆

【约束条件】
- 输出的新搜索词数量应尽量与未收敛概念数量保持一致或更少（合并时可减少），不允许无限扩展
- 不要输出已收敛概念作为新搜索词
- 使用银行业标准术语，不使用口语化表达
- 如果无法改进，输出原始概念作为新搜索词

【输出格式】
直接返回 JSON，格式为：{{"new_concepts": ["新搜索词1", "新搜索词2"], "reasoning": "诊断说明"}}
不要使用任何 markdown 代码块包裹。
""" 


NORMALIZED_QUESTION_PROMPT = """你是银行数据分析专家。请基于以下信息生成一条规范化的需求描述。

【用户原始问题】
{user_question}

【筛选条件】
{filter_phrases_str}

【用户确认的分析维度】
{converged_concepts_str}

【规范化要求】
- 将口语化表达转换为银行业标准分析语言
- **严格只使用【用户确认的分析维度】中的概念，禁止基于【用户原始问题】扩展新的分析维度**
- 将筛选条件和分析维度自然融合成一句完整的描述
- 语言简洁，不超过 100 字
- 如果【用户确认的分析维度】为空，则直接精简表述用户原始问题，去除口语化内容即可

【输出格式】
直接返回 JSON，格式为：{{"normalized_question": "规范化后的需求描述"}}
不要使用任何 markdown 代码块包裹。
"""


PHRASE_EXTRACTION_PROMPT = """你是银行数据分析专家。请从用户的自然语言问题中提取业务相关的词组，不做任何分类或加工。

{conversation_history}
【当前用户问题】
{user_question}

【提取要求】
- 仅提取与业务分析相关的词组
- 忽略语气词、连接词、标点符号
- 不要重复，不要归纳总结
- 如果当前问题是追问（如"那XX呢"、"生成XX"），请结合上一轮背景，
  将被省略的机构、时间、分析意图等信息补全提取出来

【输出格式】
必须返回以下 JSON 格式，字段名必须是 phrases：
{{"phrases": ["词组1", "词组2", "词组3"]}}
不要使用任何 markdown 代码块包裹。
重要：返回的 JSON 必须包含名为 "phrases" 的键，值必须是字符串数组。
"""


PHRASE_CLASSIFICATION_PROMPT = """你是银行数据分析专家。请对以下词组进行语义分类，判断每个词组属于"筛选值"还是"分析概念"。

【用户原始问题】
{user_question}

【待分类词组】
{phrases}

【分类标准 - 严格按以下规则执行】

▶ 筛选值（filter_phrases）—— 只有以下三类，其余一律归分析概念：
  1. 机构名称：含"分行/支行/总行/省行/营业部/中心"的词
     → 例：南京分行 ✅、上海支行 ✅
  2. 时间词：含"年/月/日/季度/上半年/下半年/近N/最近"的词
     → 例：2024年 ✅、近三个月 ✅、2025 ✅
  3. 地区行政区划：省/市/区/县等行政区划词（不含机构名称）
     → 例：江苏省 ✅、南京市 ✅

▶ 分析概念（analysis_concepts）—— 不属于以上三类的所有词：
  → 客户类型：小微企业、个人客户、对公客户 ✅
  → 产品类型：小微企业贷款、住房贷款、信用贷款 ✅
  → 业务指标：贷款余额、不良率、逾期率 ✅
  → 分析意图：风险、增长、占比、趋势 ✅

【输出格式】
直接返回 JSON：{{"filter_phrases": ["筛选值词组"], "analysis_concepts": ["分析概念词组"], "reasoning": "分类依据"}}
不要使用任何 markdown 代码块包裹。

"""


# ═══════════════════════════════════════════════════════════════════════
# 阶段 1：主题定位与指标裁决
# ═══════════════════════════════════════════════════════════════════════

THEME_JUDGMENT_PROMPT = """你是银行数据分析专家，需要从主题的全量指标中精准定位能覆盖用户需求的指标。

【用户原始问题】
{user_question}

【用户确认的分析维度】
{analysis_dimensions}

【当前主题】
主题名称: {theme_name}
主题路径: {theme_path}

【主题下全量筛选指标】
{filter_indicators_str}

【主题下全量分析指标】
{analysis_indicators_str}

【裁决任务】

第一步：主题可用性判断
判断该主题是否能够支撑用户的分析需求。
判断依据：
- 主题的业务领域是否与用户需求匹配？
- 主题下的分析指标是否覆盖用户的原始问题？
- 主题下的筛选指标是否支持用户所需的筛选条件（机构、时间等）？

**关键说明**：Smart BI 平台具备自动聚合统计能力，不需要主题中预先存在"数量""金额合计"等聚合指标。
- 如果用户想查"客户数"，只要主题中有客户属性字段（每行代表一个客户实体），平台即可自动 COUNT 出客户数。
- 如果用户想查"贷款余额合计"，只要主题中有"贷款余额"字段，平台即可自动 SUM。
- **不能因为主题没有预聚合指标（如"有效结算客户数"）就判定为不支持**，应关注主题所覆盖的业务实体本身是否与用户需求匹配。

第二步：指标精筛（仅在主题可用时执行）
从筛选指标和分析指标两部分中，分别定位出能覆盖用户分析需求的指标。

精筛原则：
- 筛选指标：保留与用户筛选条件（机构、时间）匹配的指标
- 分析指标：保留与分析用户原始问题相关的指标，排除与用户问题无关的指标
- 指标数量以完整覆盖用户需求为准

【输出格式】
直接返回 JSON，格式为：{{"theme_id": "...", "theme_name": "...", "is_supported": true/false, "support_reason": "理由", "selected_filter_indicators": [{{"indicator_id": "...", "alias": "...", "type": "...", "reason": "..."}}], "selected_analysis_indicators": [{{"indicator_id": "...", "alias": "...", "description": "...", "reason": "..."}}], "unsupported_dimensions": []}}
不要使用任何 markdown 代码块包裹。
"""


# ═══════════════════════════════════════════════════════════════════════
# 阶段 2：模板推荐
# ═══════════════════════════════════════════════════════════════════════

TEMPLATE_USABILITY_PROMPT = """你是银行数据分析专家，需要精准判断以下模板是否真正能支撑用户的分析需求。

【用户原始问题】
{user_question}

【用户确认的分析维度】
{analysis_dimensions}

【模板信息】
模板名称: {template_alias}
模板描述: {template_description}
覆盖率: {coverage_ratio}（模板已覆盖的用户需求指标比例）

【模板包含的全量指标】
{all_template_indicators_str}

【用户需要但模板缺失的指标】
{missing_indicators_str}

【裁决任务】
判断该模板是否能够支撑用户的分析需求。
判断依据：
- 模板的业务领域是否与用户需求匹配？
- 模板已有的指标是否覆盖了用户的核心分析意图？
- 覆盖率虽达到阈值，但缺失的指标是否属于核心分析维度？

**关键说明**：Smart BI 平台具备自动聚合统计能力，不需要模板中预先存在"数量""金额合计"等聚合指标。
- 如果用户想查"客户数"，只要模板中有客户属性字段（每行代表一个客户实体），平台即可自动 COUNT 出客户数。
- 如果用户想查"贷款余额合计"，只要模板中有"贷款余额"字段，平台即可自动 SUM。
- **不能因为模板没有预聚合指标就判定为不可用**，应关注模板所覆盖的业务实体本身是否与用户需求匹配。

【输出格式】
直接返回 JSON，格式为：{{"template_id": "...", "is_supported": true/false, "support_reason": "判断理由"}}
不要使用任何 markdown 代码块包裹。
"""


# ═══════════════════════════════════════════════════════════════════════
# 分析维度勾选引导
# ═══════════════════════════════════════════════════════════════════════

DIMENSION_SELECTION_GUIDANCE_PROMPT = """你是银行数据分析专家。系统已经为用户提取了多个分析维度，每个维度都会导向一组不同的推荐主题。你需要评估这些维度之间的主题方向是否一致，并为用户提供勾选建议。

【用户原始问题】
{user_question}

【预计算的加权 Jaccard 相似度矩阵】
{dimensions_str}

【各维度命中的真实主题及权重（Neo4j）】
{analysis_dimensions_str}

【业务背景说明】
本系统的目标是帮用户推荐合适的分析主题。每个分析维度会导向一组候选主题。如果两个维度导向的主题方向完全不同（加权 Jaccard < {jaccard_threshold}），同时勾选会导致系统推荐两组方向各异的主题，让用户难以聚焦。因此，建议用户优先勾选最核心的分析维度，让推荐结果更加集中。

【任务说明】
1. 查看每个维度命中的真实主题及权重（来自 Neo4j），不要仅凭别名猜测主题
2. 基于预计算的加权 Jaccard 相似度解读维度间关系：
   - 加权 Jaccard >= {jaccard_threshold} → 两个维度导向的主题方向一致，可以互补
   - 加权 Jaccard < {jaccard_threshold}  → 两个维度导向的主题方向完全不同，同时勾选会导致推荐结果分散
3. 结合主题方向和用户核心意图，给出 `recommended_first`（建议优先勾选的核心维度）
4. 注意：`has_conflict`、`can_select_all`、`selection_advice` 由程序化计算，你只需生成 `recommended_first`、`conflict_analysis` 和 `dimension_analysis`

【conflict_analysis 写作要求】
- 用通俗的业务语言描述：每个维度分别导向哪些主题方向，为什么同时选择会让推荐结果分散
- 说明后果：同时勾选方向不同的维度，系统会推荐多组互不相干的主题，让用户难以聚焦到真正需要的分析场景
- 给出建议：建议用户优先选择自己最关心的核心维度，获得聚焦的推荐后再考虑是否补充其他维度
- **禁止使用**数据库/技术术语（如"正交维度"、"笛卡尔积"、"事实表"、"数据模型"等）
- 可以提及加权 Jaccard 数值作为支撑依据

【输出格式（JSON）】
直接返回：
{{"recommended_first": ["优先勾选的维度列表"], "conflict_analysis": "维度冲突分析文字", "dimension_analysis": [{{"dimension": "维度名", "primary_theme": "权重最高的主要主题名", "independence_score": 0.0-1.0, "core_concept_score": 0.0-1.0, "recommendation": "优先/可选/建议后选"}}]}}
不要使用任何 markdown 代码块包裹。
"""

# ═══════════════════════════════════════════════════════════════════════
# 低置信度处理
# ═══════════════════════════════════════════════════════════════════════

LOW_CONFIDENCE_PROMPT = """你是银行数据分析专家。以下分析概念在多轮迭代后仍无法达到满意匹配度。

【用户原始问题】
{user_question}

【未收敛的分析概念】
{low_confidence_concepts}

【当前搜索结果】
{search_results_str}

【任务】
1. 分析每个未收敛概念无法匹配的原因
2. 提供 2~3 个换词建议，帮助用户重新描述
3. 生成一条友好的提示信息

【输出格式】
直接返回 JSON，格式为：{{"analysis": "分析", "suggestions": [{{"concept": "原概念", "reason": "原因", "alternatives": ["建议1", "建议2"]}}], "user_message": "用户提示"}}
不要使用任何 markdown 代码块包裹。
"""


# ═══════════════════════════════════════════════════════════════════════
# 阶段 3：输出格式化
# ═══════════════════════════════════════════════════════════════════════

FORMAT_OUTPUT_PROMPT = """你是银行数据分析专家。请用简洁、自然的语言将以下分析结果整理成人类易读的报告。

【用户原始问题】
{user_question}

【规范化需求】
{normalized_question}

【筛选条件】
{filter_indicators_str}

【分析维度】
{analysis_dimensions_str}

【推荐主题】
{recommended_themes_str}

【推荐模板】
{recommended_templates_str}

【输出要求】
用自然的语言写一份简洁的报告，包含：

1. 一句话说明用户的分析需求
2. 推荐 1-2 个最适合的主题，简单说明理由
3. 如果有可用的模板，简单推荐；如果没有合适的，直接说明

【格式要求】
- 使用极简的 Markdown（仅用 # 标题和 - 列表）
- 不要用任何框线字符（╔═╗┌─┐│├┤└┘等）
- 不要过度使用 emoji，只在必要时用 1-2 个
- 语言要自然流畅，像在和用户对话
- 控制在 500 字以内

直接输出内容，不要包裹在代码块中。
"""


# ═══════════════════════════════════════════════════════════════════════
# 板块筛选 Prompt
# ═══════════════════════════════════════════════════════════════════════

SECTOR_FILTER_PROMPT = """你是银行数据分析专家。请根据用户问题，从以下板块列表中筛选出与用户需求可能相关的板块。

【用户原始问题】
{user_question}

【板块列表】
{sector_list_str}

【筛选任务】
1. 分析每个板块的名称和路径，判断其业务领域是否可能与用户需求相关
2. 宁可选多不选漏：如果某个板块的业务范围可能覆盖用户的分析意图，就应该选中
3. 可以参考板块路径中的层级信息（如"资产板块"下会有贷款相关主题）
4. 只选择与用户需求明显相关的板块

【输出格式】
直接返回 JSON，格式为：{{"selected_sectors": [{{"sector_id": "...", "sector_alias": "...", "sector_path": "...", "reason": "选择理由"}}]}}
不要使用任何 markdown 代码块包裹。
"""


# ═══════════════════════════════════════════════════════════════════════
# 层级导航 Prompt
# ═══════════════════════════════════════════════════════════════════════

HIERARCHY_NAVIGATION_PROMPT = """你是银行数据分析专家。请从以下板块主题列表中筛选出与用户需求相关的候选主题。

【用户原始问题】
{user_question}

【用户确认的分析维度】
{analysis_dimensions}

【板块主题列表】
{theme_list_str}

【筛选任务】
1. 分析每个主题是否与用户需求相关
2. 结合分析维度中的指标信息，判断主题的业务领域是否覆盖用户的分析意图
3. 如果某个主题的业务范围可能覆盖用户的分析意图，就应该选中

【输出格式】
直接返回 JSON，格式为：{{"selected_themes": [{{"theme_id": "...", "theme_alias": "...", "theme_path": "...", "reason": "选择理由"}}]}}
不要使用任何 markdown 代码块包裹。
"""

