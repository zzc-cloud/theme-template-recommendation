"""
LLM Prompt 定义
从 SKILL.md 迁移所有 Prompt，统一管理
"""

# ═══════════════════════════════════════════════════════════════════════
# 阶段 0：需求澄清
# ═══════════════════════════════════════════════════════════════════════

PHRASE_EXTRACTION_PROMPT = """你是银行数据分析专家。请从用户的自然语言问题中提取业务相关的词组，不做任何分类或加工。

【用户问题】
{user_question}

【提取要求】
- 仅提取与业务分析相关的词组
- 忽略语气词、连接词、标点符号
- 不要重复，不要归纳总结

【输出格式】
直接返回 JSON，格式为：{{"phrases": ["词组1", "词组2", "词组3"]}}
不要使用任何 markdown 代码块包裹。
"""


PHRASE_CLASSIFICATION_PROMPT = """你是银行数据分析专家。请对以下词组进行语义分类，判断每个词组属于"筛选值"还是"分析概念"。

【用户原始问题】
{user_question}

【待分类词组】
{phrases}

【分类标准】
- 筛选值：机构名称（分行/支行）、时间词（年/月/日）、地区/行政区划
- 分析概念：业务术语（贷款余额/不良率）、分析意图词（风险/增长/占比）

【输出格式】
直接返回 JSON，格式为：{{"filter_phrases": ["筛选值词组"], "analysis_concepts": ["分析概念词组"], "reasoning": "分类依据"}}
不要使用任何 markdown 代码块包裹。
"""


ITERATION_EVALUATION_PROMPT = """你是银行数据分析专家，正在帮助用户精确定位分析指标。

【用户原始问题】
{user_question}

【当前轮次】第 {round} 轮（共最多 {max_rounds} 轮）

【当前分析概念及搜索结果】
{search_results_str}

【收敛标准】
满足任一条件即可收敛：
1. 每个分析概念 Top-1 相似度 > 0.80
2. 本轮修正搜索词与上轮完全相同
3. 本轮新增候选指标数 = 0

【评估任务】
1. 覆盖评估：用户意图的哪些部分已被覆盖？哪些缺失？
2. 质量判断：当前结果是否达标？（每个分析概念 Top-1 > 0.80）
3. 若不达标，诊断问题：
   - 搜索词过于宽泛？（结果太杂）→ 收缩
   - 搜索词过于口语化？（结果偏离）→ 替换
   - 搜索词缺少限定词？（召回不足）→ 扩展
   - 多个搜索词指向同一意图？→ 合并
4. 输出修正搜索词，同时输出规范化问题描述

【输出格式】
直接返回 JSON，格式为：{{"quality_pass": true/false, "converged": true/false, "coverage_assessment": "描述", "normalized_question": "规范化问题", "corrections": [{{"original": "原词", "action": "动作", "corrected": ["新词"], "reason": "原因"}}], "low_confidence_concepts": []}}
不要使用任何 markdown 代码块包裹。
"""


# ═══════════════════════════════════════════════════════════════════════
# 阶段 1：主题定位与指标裁决
# ═══════════════════════════════════════════════════════════════════════

THEME_JUDGMENT_PROMPT = """你是银行数据分析专家，需要从主题的全量指标中精准定位能覆盖用户需求的指标。

【用户原始问题】
{user_question}

【用户确认的分析维度】
{analysis_dimensions}

【当前主题】
主题名称: {theme_name}
主题路径: {theme_path}

【主题下全量筛选指标】
{filter_indicators_str}

【主题下全量分析指标】
{analysis_indicators_str}

【裁决任务】

第一步：主题可用性判断
判断该主题是否能够支撑用户的分析需求。
判断依据：
- 主题的业务领域是否与用户需求匹配？
- 主题下的分析指标是否覆盖用户的核心分析意图？
- 主题下的筛选指标是否支持用户所需的筛选条件（机构、时间等）？

第二步：指标精筛（仅在主题可用时执行）
从筛选指标和分析指标两部分中，分别定位出能覆盖用户分析需求的指标。

精筛原则：
- 筛选指标：保留与用户筛选条件（机构、时间）匹配的指标
- 分析指标：保留与用户分析维度语义相关、或有补充分析价值的指标
- 排除与用户需求无关的指标
- 指标数量不设上限，以完整覆盖用户需求为准

【输出格式】
直接返回 JSON，格式为：{{"theme_id": "...", "theme_name": "...", "is_supported": true/false, "support_reason": "理由", "selected_filter_indicators": [{{"indicator_id": "...", "alias": "...", "type": "...", "reason": "..."}}], "selected_analysis_indicators": [{{"indicator_id": "...", "alias": "...", "description": "...", "reason": "..."}}], "unsupported_dimensions": []}}
不要使用任何 markdown 代码块包裹。
"""


# ═══════════════════════════════════════════════════════════════════════
# 阶段 2：模板推荐
# ═══════════════════════════════════════════════════════════════════════

TEMPLATE_USABILITY_PROMPT = """你是银行数据分析专家。请基于用户问题，对以下模板的指标构成进行分析，判断该模板的实际可用性，并说明缺失指标的影响与补充建议。

【用户原始问题】
{user_question}

【用户确认的分析维度及关联指标】
{analysis_dimensions}

【模板信息】
模板名称: {template_alias}
模板描述: {template_description}
覆盖率: {coverage_ratio}

【模板包含的全量指标】
{all_template_indicators_str}

【缺失指标（用户需求中未被该模板覆盖的部分）】
{missing_indicators_str}

【分析任务】
1. 结合模板的全量指标与用户问题，评估该模板能在多大程度上满足用户需求
2. 针对每个缺失指标，判断其对用户分析的重要程度及缺失影响
3. 给出整体可用性结论与补充建议

【可用性等级】
- 可直接使用：缺失指标均为辅助或可忽略级别，不影响核心分析
- 补充后可用：缺失部分辅助指标，在模板基础上补充后可满足需求
- 缺口较大建议谨慎：缺失核心指标，需较多调整才能满足需求

【输出格式】
直接返回 JSON，格式为：{{"template_id": "...", "overall_usability": "等级", "usability_summary": "一句话说明", "missing_indicator_analysis": [{{"indicator_alias": "...", "importance": "重要程度", "impact": "影响", "supplement_suggestion": "建议"}}]}}
不要使用任何 markdown 代码块包裹。
"""


# ═══════════════════════════════════════════════════════════════════════
# 低置信度处理
# ═══════════════════════════════════════════════════════════════════════

LOW_CONFIDENCE_PROMPT = """你是银行数据分析专家。以下分析概念在多轮迭代后仍无法达到满意匹配度。

【用户原始问题】
{user_question}

【未收敛的分析概念】
{low_confidence_concepts}

【当前搜索结果】
{search_results_str}

【任务】
1. 分析每个未收敛概念无法匹配的原因
2. 提供 2~3 个换词建议，帮助用户重新描述
3. 生成一条友好的���示信息

【输出格式】
直接返回 JSON，格式为：{{"analysis": "分析", "suggestions": [{{"concept": "原概念", "reason": "原因", "alternatives": ["建议1", "建议2"]}}], "user_message": "用户提示"}}
不要使用任何 markdown 代码块包裹。
"""
