"""
LLM Prompt 定义
从 SKILL.md 迁移所有 Prompt，统一管理
"""

# ═══════════════════════════════════════════════════════════════════════
# 阶段 0：需求澄清
# ═══════════════════════════════════════════════════════════════════════

ITERATION_REFINEMENT_PROMPT = """你是银行数据分析专家，正在帮助用户精确定位分析指标。

【用户原始问题】
{user_question}

【当前轮次】第 {round} 轮（共最多 {max_rounds} 轮）

【未收敛概念及其搜索结果】
{pending_search_results_str}

【已收敛概念（仅供参照，无需处理）】
{converged_concepts_str}

【诊断任务】
基于已收敛的概念，针对未收敛概念及其搜索结果生成下一轮更精准的搜索词。

【诊断方向引导】
1. 搜索词过于口语化 → 替换为银行业专业术语
2. 搜索词含义模糊 → 拆分为更明确的表达（最多拆分为2个搜索词）
3. 多个概念指向同一意图 → 合并为一个更精准的词

【约束条件】
- 输出的新搜索词数量应尽量与未收敛概念数量保持一致或更少（合并时可减少），不允许无限扩展
- 不要输出已收敛概念作为新搜索词
- 使用银行业标准术语，不使用口语化表达

【输出格式】
直接返回 JSON，格式为：{{"new_concepts": ["新搜索词1", "新搜索词2"], "reasoning": "诊断说明"}}
不要使用任何 markdown 代码块包裹。
""" 


NORMALIZED_QUESTION_PROMPT = """你是银行数据分析专家。请基于以下信息生成一条规范化的需求描述。

【用户原始问题】
{user_question}

【筛选条件】
{filter_phrases_str}

【用户确认的分析维度】
{converged_concepts_str}

【规范化要求】
- 将口语化表达转换为银行业标准分析语言
- **严格只使用【用户确认的分析维度】中的概念，禁止基于【用户原始问题】扩展新的分析维度**
- 将筛选条件和分析维度自然融合成一句完整的描述
- 语言简洁，不超过 100 字
- 如果【用户确认的分析维度】为空，则直接精简表述用户原始问题，去除口语化内容即可

【输出格式】
直接返回 JSON，格式为：{{"normalized_question": "规范化后的需求描述"}}
不要使用任何 markdown 代码块包裹。
"""


PHRASE_EXTRACTION_PROMPT = """你是银行数据分析专家。请从用户的自然语言问题中提取业务相关的词组，不做任何分类或加工。

{conversation_history}
【当前用户问题】
{user_question}

【提取要求】
- 仅提取与业务分析相关的词组
- 忽略语气词、连接词、标点符号
- 不要重复，不要归纳总结
- 如果当前问题是追问（如"那XX呢"、"生成XX"），请结合上一轮背景，
  将被省略的机构、时间、分析意图等信息补全提取出来

【输出格式】
必须返回以下 JSON 格式，字段名必须是 phrases：
{{"phrases": ["词组1", "词组2", "词组3"]}}
不要使用任何 markdown 代码块包裹。
重要：返回的 JSON 必须包含名为 "phrases" 的键，值必须是字符串数组。
"""


PHRASE_CLASSIFICATION_PROMPT = """你是银行数据分析专家。请对以下词组进行语义分类，判断每个词组属于"筛选值"还是"分析概念"。

【用户原始问题】
{user_question}

【待分类词组】
{phrases}

【分类标准 - 严格按以下规则执行】

▶ 筛选值（filter_phrases）—— 只有以下三类，其余一律归分析概念：
  1. 机构名称：含"分行/支行/总行/省行/营业部/中心"的词
     → 例：南京分行 ✅、上海支行 ✅
  2. 时间词：含"年/月/日/季度/上半年/下半年/近N/最近"的词
     → 例：2024年 ✅、近三个月 ✅
  3. 地区行政区划：省/市/区/县等行政区划词（不含机构名称）
     → 例：江苏省 ✅、南京市 ✅

▶ 分析概念（analysis_concepts）—— 不属于以上三类的所有词：
  → 客户类型：小微企业、个人客户、对公客户 ✅
  → 产品类型：小微企业贷款、住房贷款、信用贷款 ✅
  → 业务指标：贷款余额、不良率、逾期率 ✅
  → 分析意图：风险、增长、占比、趋势 ✅

【输出格式】
直接返回 JSON：{{"filter_phrases": ["筛选值词组"], "analysis_concepts": ["分析概念词组"], "reasoning": "分类依据"}}
不要使用任何 markdown 代码块包裹。

"""


# ═══════════════════════════════════════════════════════════════════════
# 阶段 1：主题定位与指标裁决
# ═══════════════════════════════════════════════════════════════════════

THEME_JUDGMENT_PROMPT = """你是银行数据分析专家，需要从主题的全量指标中精准定位能覆盖用户需求的指标。

【用户原始问题】
{user_question}

【用户确认的分析维度】
{analysis_dimensions}

【当前主题】
主题名称: {theme_name}
主题路径: {theme_path}

【主题下全量筛选指标】
{filter_indicators_str}

【主题下全量分析指标】
{analysis_indicators_str}

【裁决任务】

第一步：主题可用性判断
判断该主题是否能够支撑用户的分析需求。
判断依据：
- 主题的业务领域是否与用户需求匹配？
- 主题下的分析指标是否覆盖用户的核心分析意图？
- 主题下的筛选指标是否支持用户所需的筛选条件（机构、时间等）？

第二步：指标精筛（仅在主题可用时执行）
从筛选指标和分析指标两部分中，分别定位出能覆盖用户分析需求的指标。

精筛原则：
- 筛选指标：保留与用户筛选条件（机构、时间）匹配的指标
- 分析指标：保留与用户分析维度语义相关、或有补充分析价值的指标
- 排除与用户需求无关的指标
- 指标数量不设上限，以完整覆盖用户需求为准

【输出格式】
直接返回 JSON，格式为：{{"theme_id": "...", "theme_name": "...", "is_supported": true/false, "support_reason": "理由", "selected_filter_indicators": [{{"indicator_id": "...", "alias": "...", "type": "...", "reason": "..."}}], "selected_analysis_indicators": [{{"indicator_id": "...", "alias": "...", "description": "...", "reason": "..."}}], "unsupported_dimensions": []}}
不要使用任何 markdown 代码块包裹。
"""


# ═══════════════════════════════════════════════════════════════════════
# 阶段 2：模板推荐
# ═══════════════════════════════════════════════════════════════════════

TEMPLATE_USABILITY_PROMPT = """你是银行数据分析专家。请基于用户问题，对以下模板的指标构成进行分析，判断该模板的实际可用性，并说明缺失指标的影响与补充建议。

【用户原始问题】
{user_question}

【用户确认的分析维度】
{analysis_dimensions}

【模板信息】
模板名称: {template_alias}
模板描述: {template_description}
覆盖率: {coverage_ratio}

【模板包含的全量指标】
{all_template_indicators_str}

【缺失指标（用户需求中未被该模板覆盖的部分）】
{missing_indicators_str}

【分析任务】
1. 结合模板的全量指标与用户问题，评估该模板能在多大程度上满足用户需求
2. 针对每个缺失指标，判断其对用户分析的重要程度及缺失影响
3. 给出整体可用性结论与补充建议

【可用性等级】
- 可直接使用：缺失指标均为辅助或可忽略级别，不影响核心分析
- 补充后可用：缺失部分辅助指标，在模板基础上补充后可满足需求
- 缺口较大建议谨慎：缺失核心指标，需较多调整才能满足需求

【输出格式】
直接返回 JSON，格式为：{{"template_id": "...", "overall_usability": "等级", "usability_summary": "一句话说明", "missing_indicator_analysis": [{{"indicator_alias": "...", "importance": "重要程度", "impact": "影响", "supplement_suggestion": "建议"}}]}}
不要使用任何 markdown 代码块包裹。
"""


# ═══════════════════════════════════════════════════════════════════════
# 分析维度勾选引导
# ═══════════════════════════════════════════════════════════════════════

DIMENSION_SELECTION_GUIDANCE_PROMPT = """你是银行数据分析专家。请基于各维度命中的真实主题和预计算的 Jaccard 相似度，分析维度间的独立性，为用户提供勾选顺序建议。

【用户原始问题】
{user_question}

【预计算的 Jaccard 相似度矩阵】
{dimensions_str}

【各维度命中的真实主题（Neo4j）】
{analysis_dimensions_str}

【任务说明】
1. 查看每个维度命中的真实主题（来自 Neo4j），不要仅凭别名猜测主题
2. 基于预计算的 Jaccard 相似度解读维度间关系：
   - Jaccard >= 0.5 → 主题高度重叠，两个维度互补性强（可同时勾选）
   - Jaccard < 0.5  → 主题几乎无交集，存在主题交叉干扰（建议优先勾选核心维度）
3. 结合 Jaccard 结果和用户核心意图，给出勾选建议

【输出格式（JSON）】
直接返回：
{{"has_conflict": true/false, "recommended_first": ["优先勾选的维度列表"], "conflict_analysis": "维度A命中了X个主题，维度B命中了Y个主题，Jaccard=xx（<0.5=有冲突），冲突原因描述", "selection_advice": "勾选建议（1-2句话）", "dimension_analysis": [{{"dimension": "维度名", "matched_themes": ["主题1", "主题2"], "theme_count": N, "independence_score": 0.0-1.0, "core_concept_score": 0.0-1.0, "recommendation": "优先/可选/建议后选"}}]}}
不要使用任何 markdown 代码块包裹。
"""

# ═══════════════════════════════════════════════════════════════════════
# 低置信度处理
# ═══════════════════════════════════════════════════════════════════════

LOW_CONFIDENCE_PROMPT = """你是银行数据分析专家。以下分析概念在多轮迭代后仍无法达到满意匹配度。

【用户原始问题】
{user_question}

【未收敛的分析概念】
{low_confidence_concepts}

【当前搜索结果】
{search_results_str}

【任务】
1. 分析每个未收敛概念无法匹配的原因
2. 提供 2~3 个换词建议，帮助用户重新描述
3. 生成一条友好的提示信息

【输出格式】
直接返回 JSON，格式为：{{"analysis": "分析", "suggestions": [{{"concept": "原概念", "reason": "原因", "alternatives": ["建议1", "建议2"]}}], "user_message": "用户提示"}}
不要使用任何 markdown 代码块包裹。
"""


# ═══════════════════════════════════════════════════════════════════════
# 阶段 3：输出格式化
# ═══════════════════════════════════════════════════════════════════════

FORMAT_OUTPUT_PROMPT = """你是银行数据分析专家。请用简洁、自然的语言将以下分析结果整理成人类易读的报告。

【用户原始问题】
{user_question}

【规范化需求】
{normalized_question}

【筛选条件】
{filter_indicators_str}

【分析维度】
{analysis_dimensions_str}

【推荐主题】
{recommended_themes_str}

【推荐模板】
{recommended_templates_str}

【输出要求】
用自然的语言写一份简洁的报告，包含：

1. 一句话说明用户的分析需求
2. 推荐 1-2 个最适合的主题，简单说明理由
3. 如果有可用的模板，简单推荐；如果没有合适的，直接说明

【格式要求】
- 使用极简的 Markdown（仅用 # 标题和 - 列表）
- 不要用任何框线字符（╔═╗┌─┐│├┤└┘等）
- 不要过度使用 emoji，只在必要时用 1-2 个
- 语言要自然流畅，像在和用户对话
- 控制在 500 字以内

直接输出内容，不要包裹在代码块中。
"""
